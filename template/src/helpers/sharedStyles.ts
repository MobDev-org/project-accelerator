import { StyleSheet } from "react-native";

export const shadows = StyleSheet.create({
  shadow8: {
    backgroundColor: "#fff",
    shadowColor: "#ccc",
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.45,
    shadowRadius: 6.65,

    elevation: 8,
  },
});
