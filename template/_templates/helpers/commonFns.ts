export const toUppercase = (name: string) => {
  return name.charAt(0).toUpperCase() + name.slice(1);
};

export const toLowerCase = (name: string) => {
  return name.charAt(0).toLowerCase() + name.slice(1);
};
